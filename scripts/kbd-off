#!/bin/sh
# Turn off keyboard backlight via daemon
set -eu
if [ ! -f "/run/kbd-rgbd/cmd" ]; then
    echo "error: daemon not running (no /run/kbd-rgbd/cmd)" >&2
    exit 1
fi
printf 'profile off\n' > /run/kbd-rgbd/cmd
